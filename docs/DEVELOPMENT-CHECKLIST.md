# قائمة متابعة تطوير منصة رواد

آخر تحديث: 2026-06-30

## طريقة التحديث

- استخدم `[ ]` للمهمة غير المنجزة.
- استخدم `[~]` للمهمة قيد التنفيذ.
- استخدم `[x]` للمهمة المنجزة.
- بعد كل تعديل برمجي، حدّث القسم المناسب وأضف سطراً في "سجل التحديثات".
- لا تُغلق أي مهمة إلا بعد تشغيل التحقق المناسب: `type-check` أو `build` أو اختبار يدوي واضح.

## الأولويات الحرجة

- [ ] توحيد حساب الأثر في مصدر واحد فقط عبر `src/lib/impact-scoring.ts`.
- [ ] إزالة معادلات النقاط المكررة من واجهة لوحة الأثر والتقارير والمكافآت.
- [ ] جعل بونص الجودة والمستويات وشرائح المكافآت تُقرأ فعلياً من `ImpactSettings`.
- [ ] إصلاح تحديث حقول أثر العضو: `networkRole`, `joinDate`, `impactNote`.
- [ ] إضافة/تعديل API واضح لتحديث بيانات أثر العضو.
- [ ] توحيد مسار إضافة العضو بين `/admin/members` وتبويب أعضاء لوحة الأثر.
- [ ] توحيد صفحة الأعضاء بالكامل: إما جعل `/admin/members` تعيد التوجيه إلى `/admin/impact?tab=members` أو العكس، لتجنب صفحتين منفصلتين تديران نفس البيانات.
- [ ] إضافة صلاحيات فعلية حسب الدور داخل APIs الحساسة.
- [ ] ربط التقييمات (Evaluations) بلوحة الأثر: تحويل نتيجة التقييم إلى نقاط أثر تلقائياً مع خيار دعم يدوي، وتفعيل `evaluationId` في نموذج ImpactLog.
- [ ] إضافة sync تلقائي: عند تسجيل Participation أو Enrollment جديدة، يُنشأ ImpactLog مقترن تلقائياً لمنع الاعتماد على استدعاء dashboard API اليدوي.
- [ ] ربط تحديث Journey تلقائياً عند إنشاء ImpactLog جديد بدل الاعتماد على زر "إعادة تحليل" اليدوي فقط.

## لوحة الأثر

- [ ] مراجعة دالة `finalPoints` لتقبل إعدادات الجودة الديناميكية.
- [ ] توحيد حساب نقاط العضو في جميع التبويبات.
- [ ] توحيد حساب الاستحقاق الشهري مع `rewardEligibility`.
- [ ] توحيد حساب التقارير الشهرية والسنوية مع محرك الأثر.
- [ ] إظهار معادلة الحساب للمستخدم داخل التقارير أو بطاقة العضو.
- [ ] إضافة اختبار وحدات لمحرك الأثر: معتمد، قيد مراجعة، مرفوض، جودة، خصومات.
- [ ] إضافة اختبار لحالات البوابات الشهرية والمكافآت.
- [ ] مراجعة أهلية العمرة والتأكد من أنها تستخدم الإعدادات المحفوظة.
- [ ] تحسين أداء تحميل لوحة الأثر: تحميل البيانات حسب التبويب النشط (lazy loading) بدل جلب 6 APIs دفعة واحدة عند فتح الصفحة.
- [ ] إضافة مؤشر اتجاه (trend indicator) لمقارنة أداء الشهر الحالي بالشهر السابق.
- [ ] إظهار منصة العضو باسمها العربي لا بالمعرف التقني في جداول لوحة الأثر.
- [ ] إضافة فلتر حسب المنصة في التبويب الرئيسي وجدول الترتيب.

## إدخال الأنشطة

- [ ] تحسين نموذج تسجيل النشاط ليشمل اختيار المنصة.
- [ ] إضافة اختيار البرنامج عند اختيار المنصة.
- [ ] إضافة اختيار النشاط/الدورة عند اختيار البرنامج.
- [ ] ملء المنصة والبرنامج تلقائياً عند ربط النشاط بمشاركة أو تسجيل.
- [ ] إضافة معاينة فورية للنقاط قبل الحفظ.
- [ ] إضافة سبب الاعتماد أو الرفض عند تغيير حالة النشاط.
- [ ] منع إدخال عدد صفر أو قيم غير منطقية.
- [ ] تحسين البحث عن العضو في نموذج النشاط بدلاً من قائمة طويلة فقط.
- [ ] دعم إدخال جماعي لأنشطة متكررة لعدة أعضاء.
- [ ] إضافة زر "سجل نشاط سريع" بجانب كل عضو في جدول الأعضاء لفتح النموذج مع اختيار العضو مسبقاً.
- [ ] إضافة حقل المصدر (sourceType) في نموذج الإدخال ليتمكن المدير من تحديد: يدوي، مشاركة، تسجيل، تقرير، تقييم، خارجي.
- [ ] إظهار إجمالي نقاط القائمة المصفاة بشكل بارز مع عدد الأنشطة المعروضة.
- [ ] تمكين فلترة الأنشطة حسب حالة الاعتماد (معتمد، قيد المراجعة، مرفوض) بالإضافة للفلترة الحالية بالعضو والمحور.

## الأعضاء والمستفيدون

- [ ] تحديد صفحة واحدة رئيسية لإضافة العضو.
- [ ] جعل أي زر "إضافة عضو" يستخدم نفس نموذج البيانات أو يعيد للصفحة الرئيسية.
- [ ] إضافة حقول أثر العضو إلى validation أو فصلها في schema خاص.
- [ ] عرض المنصة باسمها لا بالمعرف في تبويب أعضاء لوحة الأثر.
- [ ] ربط العضو بمنصة أو أكثر بطريقة واضحة إن كان ذلك مطلوباً تشغيلياً.
- [ ] إضافة حالة اكتمال ملف العضو.
- [ ] إضافة سجل زمني موحد للعضو: تسجيل، مشاركة، أثر، مكافآت.

## مستخدمو النظام والصلاحيات

- [ ] مراجعة صلاحيات `SUPER_ADMIN`, `ADMIN`, `EDITOR`.
- [ ] منع `EDITOR` من الحذف والاعتماد النهائي.
- [ ] إضافة دور مخصص لمراجعة الأثر إن لزم: `IMPACT_REVIEWER`.
- [ ] إخفاء روابط الواجهة حسب صلاحية المستخدم.
- [ ] حماية APIs بنفس قواعد الواجهة، وليس بالواجهة فقط.
- [ ] تسجيل عمليات إنشاء وتعديل وحذف مستخدمي النظام في سجل النشاط.
- [ ] إزالة أو تقييد زر الدخول التجريبي في بيئة الإنتاج.

## التقارير والتحليلات

- [ ] توحيد كل أرقام التقارير مع محرك الأثر.
- [ ] إضافة تصدير Excel لتقارير الأثر.
- [ ] إضافة تصدير PDF لتقرير عضو وتقرير شهر.
- [ ] إضافة فلترة حسب المنصة والبرنامج والصفة والفترة.
- [ ] إضافة مقارنة شهرية للأعضاء والمنصات.
- [ ] إضافة تنبيه عند وجود أنشطة قيد المراجعة تؤثر على التقرير.
- [ ] توثيق هل التقرير يحتسب الأنشطة المعتمدة فقط أو كل الأنشطة.
- [ ] إضافة تقرير "فائزو الفترة" يظهر: رائد الفترة، مؤثر الفترة، باحث الفترة، متطوع الفترة.
- [ ] إضافة ملخص تنفيذي ذكي في رأس التقرير (أبرز الأرقام + العضو المتصدر + الأنشطة الأكثر شيوعاً).
- [ ] إظهار جدول أداء المنصات في التقارير غير المخصصة لمنصة واحدة.

## جودة البيانات

- [ ] توحيد أسماء الكيانات في سجل النشاط: `impactLog` أو `impact_log` وليس خليطاً.
- [ ] إضافة validation لمسارات `impact/actions`, `impact/logs`, `impact/awards`, `impact/gates`.
- [ ] منع حذف نوع نشاط مستخدم في سجلات أثر، أو تحويل الحذف إلى تعطيل.
- [ ] إضافة قيود أو رسائل واضحة للتكرار في `ImpactLog`.
- [ ] مراجعة `sourceType/sourceId/actionId` لمنع ازدواج احتساب الأنشطة التشغيلية.
- [ ] إضافة seed production-safe منفصل عن seed التجريبي.
- [ ] إزالة أو أرشفة APIs القديمة غير المستخدمة: `/api/admin/beneficiaries` و `/api/admin/team` إن لم تعد مستخدمة.
- [ ] مراجعة استخدام الحقل `platformId` في Beneficiary: هل العضو ينتمي لمنصة واحدة أم عدة منصات؟ تحديد العلاقة بوضوح في الـ Schema.
- [ ] التأكد من أن النقاط التلقائية من Participation/Enrollment لا تتضاعف عند استدعاء dashboard API أكثر من مرة.
- [ ] إضافة migrate آمن للإنتاج مع التأكد من عدم وجود `--accept-data-loss` ضروري.

## تجربة المستخدم

- [ ] تبسيط لوحة الأثر إلى مسار عمل واضح: عضو -> نشاط -> مراجعة -> تقرير.
- [ ] إضافة شاشة "مهام تنتظر الاعتماد".
- [ ] إضافة مؤشرات نجاح بعد كل حفظ مع رابط للعنصر.
- [ ] تحسين رسائل الخطأ القادمة من API.
- [ ] إضافة حالات تحميل وفارغ وفشل متسقة لكل صفحات الإدارة.
- [ ] تحسين قابلية الاستخدام على الجوال للجدول الكبير في لوحة الأثر.
- [ ] توحيد المصطلحات: عضو، مستفيد، مستخدم نظام.

## البنية والبناء

- [ ] إزالة `typescript.ignoreBuildErrors` بعد استقرار الفحص.
- [ ] إزالة `eslint.ignoreDuringBuilds` بعد إصلاح lint.
- [ ] ضبط سكربت build مع `NODE_OPTIONS=--max-old-space-size=4096` إن بقيت الحاجة.
- [ ] إضافة سكربت تحقق موحد: type-check + build.
- [ ] مراجعة متغيرات البيئة المحلية والإنتاجية، خاصة `AUTH_URL`.
- [ ] فصل إعدادات التطوير عن الإنتاج بوضوح.
- [ ] إضافة README تشغيلي يوضح تسجيل الدخول والإعدادات الأساسية.

## التحقق والاختبار

- [ ] تشغيل `npx tsc --noEmit --pretty false` بعد كل تعديل TypeScript.
- [ ] تشغيل `npm run build` قبل أي تسليم كبير.
- [ ] اختبار تسجيل الدخول محلياً.
- [ ] اختبار إضافة مستخدم نظام.
- [ ] اختبار إضافة عضو.
- [ ] اختبار تسجيل نشاط واعتماده وظهور نقاطه.
- [ ] اختبار تقرير شهري بعد إضافة نشاط.
- [ ] اختبار تعطيل البوابة الشهرية وتأثيرها على الاستحقاق.

## سجل التحديثات

### 2026-06-30

- [x] إنشاء قائمة متابعة تطوير مركزية في `docs/DEVELOPMENT-CHECKLIST.md`.
- [x] توثيق أولويات لوحة الأثر، الإدخال، الصلاحيات، التقارير، وجودة البيانات.
- [x] إضافة بنود ازدواجية صفحة الأعضاء وربط التقييمات وsync التلقائي (من تقرير الفحص الشامل).
- [x] إضافة بنود تحسين أداء التحميل، فلترة متقدمة، وإدخال سريع للنشاط.
- [x] إضافة بنود تقارير فائزي الفترة، الملخص التنفيذي الذكي، وتصدير Excel/PDF.
- [x] إضافة بنود APIs القديمة، علاقة العضو بالمنصة، ومنع تضاعف النقاط التلقائية.
